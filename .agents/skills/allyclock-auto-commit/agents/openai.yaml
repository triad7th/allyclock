interface:
  display_name: "AllyClock Auto Commit"
  short_description: "Create grouped AllyClock commits consistently"
  default_prompt: "Use $allyclock-auto-commit to review current changes, split them into logical commits when needed, validate them, and commit them."
