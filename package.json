{
  "name": "allyclock-workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix apps/web run start",
    "build": "npm --prefix apps/web run build",
    "watch": "npm --prefix apps/web run watch",
    "test": "npm --prefix apps/web run test",
    "start:web": "npm --prefix apps/web run start",
    "build:web": "npm --prefix apps/web run build",
    "watch:web": "npm --prefix apps/web run watch",
    "test:web": "npm --prefix apps/web run test",
    "lint:web": "npm --prefix apps/web run lint",
    "format:web": "npm --prefix apps/web run format",
    "format:check:web": "npm --prefix apps/web run format:check"
  }
}
